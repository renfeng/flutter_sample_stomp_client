import 'package:usage/usage.dart';

Future<Analytics> analytics({
  trackingId,
  applicationName,
  applicationVersion,
  analyticsUrl,
}) =>
    throw UnsupportedError("Nether web nor native support is available.");
